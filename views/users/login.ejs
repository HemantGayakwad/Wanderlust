<% layout("layouts/boilerplate") %>
<div class="row mt-3">
    <h1 class="col-6 offset-3">login</h1>
    <div class="col-6 offset-3">
        <form action="/login" method="post" class="needs-validation" nonvaidate>
            <div class="mb-3">
                <label for="username" class="form-label"> Username</label>
                <input 
                name = "username"
                type="text"
                class="form-control"
                required
                />
            <div class="mb-3">
                <label for="password" class="form-label"> password</label>
                <input 
                    name = "password"
                    type="password"
                    class="form-control"
                    required
                />
            <button class="btn btn-success mt-3"> login</button>
        </form>
    </div>
</div>